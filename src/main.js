import { App } from "app";
import { Factory } from "factory";
import { View } from "view";
import { Route } from "route";
import { Store } from "store";

return {
	App: App,
	Factory: Factory,
	View: View,
	Route: Route,
	Store: Store
};
